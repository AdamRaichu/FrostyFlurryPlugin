<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:core="clr-namespace:Frosty.Core.Controls;assembly=FrostyCore"
    xmlns:ctrl="clr-namespace:Frosty.Controls;assembly=FrostyControls"
    xmlns:local="clr-namespace:Flurry.Editor.Editors"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:fdev="FrostyDeveloper">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/FrostyControls;component/Themes/Generic.xaml"/>
    </ResourceDictionary.MergedDictionaries>
    <Style
		TargetType="{x:Type local:KyberLaunchOverridesEditor}"
		x:Key="{x:Type local:KyberLaunchOverridesEditor}">
        <Setter
			Property="Template">
            <Setter.Value>
                <ControlTemplate
					TargetType="{x:Type local:KyberLaunchOverridesEditor}">
                    <Grid
						Background="{StaticResource ControlBackground}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition
								Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition
                                Height="24" />
                            <RowDefinition
                                Height="*" />
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0"
                              Grid.Column="0"
                              HorizontalAlignment="Left">
                            <Button Content="Refresh 'Available Mods'" Width="150" x:Name="RefreshAvailableMods"></Button>
                        </Grid>
                        <ctrl:FrostyDockablePanel
                            Grid.Row="1"
                            Grid.Column="0"
							TitleBarVisible="False">
                            <Grid>
                                <core:FrostyPropertyGrid
									Name="PART_LoadOrderEditor"
									ClassViewVisible="True" />
                            </Grid>
                        </ctrl:FrostyDockablePanel>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>